/// task model (id, title,  content, result)
class Task {
  final String id;
  final String title;
  final String content;
  final String result;

  Task(this.id, this.title, this.content, this.result);
}